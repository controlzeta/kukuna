@page
@model WebUI.Pages.Ingredients.IndexModel
@{
    ViewData["Title"] = "Lista de Compra";
}
<p>
    <a asp-page="Create">Agregar Ingrediente</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ingredient[0].IngredientName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ingredient[0].Category)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Ingredient)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IngredientId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IngredientName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Category)
                </td>
                <td>
                    <a class="button" asp-page="./Edit" asp-route-id="@item.IngredientId">Editar</a> |
                    <a class="button" asp-page="./Details" asp-route-id="@item.IngredientId">Detalle</a> |
                    <a class="button" asp-page="./Delete" asp-route-id="@item.IngredientId">Borrar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
